/**
 * the custom render file
 * options for koa-views
 * set extension value if not html
 */

let options = {
    map: {
        pug: 'pug',
    },
    extension: 'pug',
};

module.exports = options;
